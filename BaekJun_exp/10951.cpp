#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>

int a, b;

int main () {

    while (1) {
        scanf("%d %d", a, b);

        if (a == ) {
            break;
        }

        printf("%d\n", );
    }

return 0;
}